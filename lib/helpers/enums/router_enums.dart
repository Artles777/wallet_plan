enum RouterNames {
  home,
  history,
  analytic,
  plans,
}

enum HomeRouterNames {
  addCard,
  transactions,
  editAccount,
  editCharts,
}

enum ChartSelected {
  liner,
  bar,
  pie,
}

enum CategoryDirection {
  left,
  right,
}

enum DrawerRoutes {
  categories,
  news,
  settings,
  help,
}